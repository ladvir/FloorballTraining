@page "/addactivity"

@inject NavigationManager NavigationManager

<AddActivityForm OnFormClosed="NavigateToActivities" IsModal="false" />

@code {

    public void NavigateToActivities()
    {
        NavigationManager.NavigateTo("/activities");
    }
}
