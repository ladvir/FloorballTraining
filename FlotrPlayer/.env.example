# Override the API base URL the app talks to (defaults to a same-machine dev URL;
# the Android emulator alias 10.0.2.2 is already substituted for `localhost` in code).
EXPO_PUBLIC_API_URL=https://localhost:5210
