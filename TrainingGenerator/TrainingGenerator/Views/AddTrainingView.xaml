<UserControl x:Class="TrainingGenerator.Views.AddTrainingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:custom="clr-namespace:LoadingSpinnerControl;assembly=LoadingSpinnerControl"
             xmlns:converters="clr-namespace:TrainingGenerator.Converters"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Style="{StaticResource Header}" Text="Nový trénink" />
            <Button Grid.Row="0" Content="Aktivity" Grid.Column="1" Margin="15 0 0 0" Command="{Binding GenerateTrainingCommand}" />
        </Grid>

        <custom:LoadingSpinner Diameter="80" Grid.Row="1"
                IsLoading="{Binding IsSubmitting}"
                Thickness="5"
                Color="Black"
                Margin="100"
                Visibility="{Binding IsSubmitting, Converter={StaticResource BooleanToVisibilityConverter}}" />

        <Grid Grid.Row="1" Margin="0 10 0 0 " Visibility="{Binding IsSubmitting, Converter={StaticResource InverseBooleanToVisibilityConverter}}">

            <StackPanel Orientation="Vertical" Grid.Row="0" Grid.Column="0">
                <StackPanel Orientation="Horizontal" Width="300">
                    <TextBlock Grid.Row="0" Text="Název" />
                    <TextBox  Margin="0 5 0 0" Width="200" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" Width="150" Margin="15 0 0 0">
                    <TextBlock Text="Počet osob min." />
                    <TextBox Margin="0 5 0 0" Text="{Binding PersonsMin, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="15 0 0 0" Width="100">
                    <TextBlock   Text="Počet osob max." />
                    <TextBox Text="{Binding PersonsMax, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="15 0 0 0" Width="100">
                    <TextBlock Text="Trvání" />
                    <TextBox Width="80" Text="{Binding Duration, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="15 0 0 0" Width="200">
                    <TextBlock Text="Zastoupení florbalu" />
                    <Slider Margin="15 0 0 0"  IsSelectionRangeEnabled="True"  Minimum="0"  Width="100" Maximum="100" SmallChange="1" LargeChange="10" Value="{Binding FlorbalPercent, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBox Margin="15 0 0 0" Width="80" Text="{Binding FlorbalPercent, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="15 0 0 0">
                    <TextBlock Text="Poznámka" />
                    <TextBox  Width="80" Text="{Binding Note, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <!--<StackPanel Orientation="Horizontal" Margin="15 0 0 0">
                    <TextBlock Text="Délka úvodu" />

                    <TextBox  Text="{Binding Duration, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="15 0 0 0">
                    <TextBlock Text="" />
                    <TextBox  Text="{Binding Duration, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="15 0 0 0">
                    <TextBlock Text="" />
                    <TextBox  Text="{Binding Duration, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="15 0 0 0">
                    <TextBlock Text="" />
                    <TextBox  Text="{Binding Duration, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="15 0 0 0">
                    <TextBlock Text="" />
                    <TextBox  Text="{Binding Duration, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>-->
            </StackPanel>

            <!--PrefferedAktivityRatioMin,
            Note,
            RatingSum,
            RatingCount,
            BeginTimeMin,
            BeginTimeMax,
            WarmUpTimeMin,
            WarmUpTimeMax,
            WarmUpExcerciseTimeMin,
            WarmUpExcerciseTimeMax,
            DrilTimeMin,
            DrilTimeMax,
            StretchingTimeMin,
            StretchingTimeMax,
            EndTimeMin,
            EndTimeMax,
            BlockPauseTimeMin,
            BlockPauseTimeMax,
            ActivityPauseTimeMin,-->
        </Grid>

        <StackPanel
            Grid.Row="3"
            Margin="0 25 0 0"
            Orientation="Horizontal">
            <Button Command="{Binding SaveCommand}" Content="Ulož" />
            <Button
                Margin="10 0 0 0"
                Command="{Binding CancelCommand}"
                Content="Cancel">
                <Button.Style>
                    <Style BasedOn="{StaticResource {x:Type Button}}" TargetType="Button">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsSubmitting}" Value="True">
                                <Setter Property="IsEnabled" Value="False" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <custom:LoadingSpinner
                Margin="10 0 0 0"
                VerticalAlignment="Center"
                Diameter="25"
                IsLoading="{Binding IsSubmitting}"
                Thickness="3"
                Color="Black" />
        </StackPanel>

        <TextBlock
            Grid.Row="4"
            Margin="0 20 0 0"
            Foreground="Red"
            Text="{Binding SubmitErrorMessage}"
            Visibility="{Binding HasSubmitErrorMessage, Converter={StaticResource BooleanToVisibilityConverter}}" />
    </Grid>
</UserControl>